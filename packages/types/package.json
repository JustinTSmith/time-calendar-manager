{
  "name": "@time-calendar-manager/types",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "node -e \"console.log('types test placeholder')\"",
    "lint": "node -e \"console.log('types lint placeholder')\""
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
